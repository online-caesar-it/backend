export enum EScheduleStatus {
  SCHEDULED = "scheduled",
  CANCELED = "canceled",
  MOVED = "moved",
}
export enum EScheduleTransferStatus {
  PENDING = "pending",
  APPROVED = "approved",
  REJECTED = "rejected",
}
